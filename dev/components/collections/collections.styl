//author @huntbao
.mod-collection-actions {
  padding: 6px 12px 4px;
  margin: 0 -10px;
  color: #ccc
  text-align: right
  border-bottom 1px solid #ccc
  transition all ease .3s

  .icon-wrap {
    cursor: pointer
  }

  &:hover {
    color: #666
  }

  .glyphicon-briefcase {
    font-size: 20px
  }

  .glyphicon-plus {
    font-size: 12px
    margin: 0 0 0 2px
  }
}

.mod-collections {
  overflow-y: auto;
  overflow-x: hidden;
  position: absolute
  top: 95px
  right: 0
  bottom: 0
  left: 0;
  padding 0 10px

  .empty-tip {
    font-size: 12px
    color: #ccc
    margin: 8px 0 0
    cursor: default
  }

  .coll {
    margin: 0 -10px
    position relative
    border-bottom: 1px solid #f5f5f5;

    &-nei {
      .coll-icon {
        border: 2px solid #1acaaf;
        color: #1acaaf;
      }

      .coll-folder-icon {
        color: #1acaaf;
      }
    }

    &-wrap {
      display: flex
      color: #4c4c4c
      padding: 12px 8px 5px
      cursor: pointer
      transition all ease .5s

      &:hover {
        background-color: #e8f2fe

        .coll-actions {
          opacity: 1
        }
      }

      .mod-dropdown-menu {
        position: absolute
        right: 21px;
        top: 66px;
        opacity: 0
        pointer-events none
        transition opacity ease .2s
      }

      &.show-action-menu {
        .mod-dropdown-menu {
          opacity: 1
          pointer-events auto
        }
      }
    }

    &-icon {
      width: 44px
      height: 44px
      line-height: 48px
      border: 2px solid #b5d5ff
      border-radius 50%
      color: #53a2f2
      font-size: 26px;
      text-align: center;
    }

    &-info {
      margin: 4px 0 0 12px
      flex: 1
    }

    &-actions,
    &-folder-actions,
    &-req-actions {

      opacity 0
      transition all ease .3s;

      &-expand-detail,
      &-menus {
        width: 28px;
        height: 28px;
        line-height: 28px;
        font-size: 16px;
        border: 1px solid #ccc
        border-radius 50%;
        text-align: center
        cursor: pointer
        color: #4c4c4c
        transition all ease .3s;

        &:hover {
          border-color #4c4c4c
        }
      }

      &-expand-detail {
        margin: -7px 0 2px

        .glyphicon-arrow-left {
          display: none
        }

        &.expand {
          .glyphicon-arrow-right {
            display: none
          }

          .glyphicon-arrow-left {
            display: block
          }
        }
      }

      &-menus {
        .glyphicon-option-horizontal {
          margin: 7px 0 0 -1px
        }
      }
    }

    &-folder-actions {
      margin: 0 -6px 0 0;
    }

    &-name {
      font-weight: bold
    }

    &-reqnum {
      font-size: 12px
      color: #ccc
      display: flex
    }

    &-dot {
      width: 8px
      height: 8px
      background-color: #ccc
      border-radius 50%
      margin: 6px 3px 0 0
    }

    &-date {
      font-size: 12px
      margin: 0 0 0 6px
    }

    &-folder {

      &-wrap {
        display: flex
        font-size: 12px
        padding: 4px 14px;
        cursor: pointer
        transition all ease .5s

        &:hover {
          background-color: #e8f2fe

          .coll-folder-actions {
            opacity: 1
          }
        }

        .glyphicon-folder-open {
          display none
          margin: 2px 0 0
        }

        &.expand {

          .glyphicon-folder-close {
            display none
          }

          .glyphicon-folder-open {
            display block
          }
        }

        .mod-dropdown-menu {
          position: absolute
          right: 21px;
          opacity: 0
          transition opacity ease .2s
          pointer-events none
        }

        &.show-action-menu {
          .mod-dropdown-menu {
            opacity: 1
            pointer-events auto
          }
        }
      }

      &-icon {
        width: 32px
        height: 32px
        line-height: 34px
        font-size: 26px
        text-align: center
        color: #53a2f2
      }

      &-name {
        margin: 7px 0 0 20px
        flex: 1;
      }

      .no-request-tip {
        margin: 4px 18px 0
        color: #999
        font-size: 12px
      }
    }

    &-req {
      color: #4c4c4c
      font-size: 12px
      display: flex
      padding: 0 0 0 20px
      height: 30px
      line-height: 30px
      cursor: pointer
      transition all ease .5s

      &:hover {
        background-color: #e8f2fe

        .coll-req-actions {
          opacity: 1
        }
      }

      &.active {
        background-color: #1acaaf;
        color: #fff
        font-weight: bold;
        cursor: default

        .coll-req-method {
          color: #fff
        }

        .coll-req-actions {
          color: #fff
          .coll-req-actions-menus {
            border-color: #fff;
            color: #fff;
          }
        }
      }

      &-method {
        margin: 0 8px 0 0
        color: #666
        font-weight: bold
        width: 44px
        text-align: center
      }

      &-url {
        width: 200px
        white-space: nowrap
        text-overflow ellipsis
        overflow: hidden
      }

      .mod-dropdown-menu {
        position: absolute
        right: 21px;
        opacity: 0
        transition opacity ease .2s
        pointer-events none
        font-weight: normal
      }

      &.show-action-menu {
        .mod-dropdown-menu {
          opacity: 1
          pointer-events auto
        }
      }
      .coll-req-actions {
        .coll-req-actions-menus {
          width: 20px;
          height: 20px;
          line-height: 20px
          font-size: 12px;
          margin: 4px 0 0;

          .glyphicon-option-horizontal {
            margin-top: 4px
          }
        }
      }

      .coll-req-url {
        width: 214px
      }
    }

    .method-get {
      color: #81d4a5
    }

    .method-post {
      color: #ffaf3d
    }

    .method-delete {
      color: #ff615f
    }

    .method-put {
      color: #83b2e5
    }

    &-folders,
    &-reqs {
      transition all ease .2s;
      height: 0
      overflow: hidden;
    }

    &-folders {
      box-shadow: inset 0 2rem 1rem -2rem rgba(0, 0, 0, 0.37);
    }

  }
}